import App from './App';
import React from 'react';
import { shallow } from 'enzyme';

describe('App', () => {

  it('should render without crashing', () => {
    shallow(<App />)
  })

  it('should call rider and games methods on load', () => {
    
  })

  it('should get the games', () => {
    
  })

})
